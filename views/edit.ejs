<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post</title>
</head>
<body>
<style>
/* General Styles */
body {
  font-family: 'Arial', sans-serif;
  margin: 0;
  padding: 0;
  background: #f4f4f9;
  color: #333;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  text-align: center;
}

/* Form Container */
form {
  background: #ffffff;
  padding: 30px;
  border-radius: 8px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
  width: 100%;
  max-width: 500px;
  margin-top: 20px;
}

h2 {
  font-size: 2em;
  margin-bottom: 10px;
  color: #4a90e2;
}

p {
  font-size: 1em;
  color: #555;
}

/* Form Fields */
div {
  margin-bottom: 15px;
  text-align: left;
}

label {
  display: block;
  font-weight: bold;
  margin-bottom: 5px;
  color: #555;
}

input[type="text"], 
input[type="tel"], 
input[type="date"] {
  width: 100%;
  padding: 10px;
  font-size: 1em;
  border: 1px solid #ccc;
  border-radius: 4px;
  transition: border-color 0.3s, box-shadow 0.3s;
}

input[type="text"]:focus, 
input[type="tel"]:focus, 
input[type="date"]:focus {
  border-color: #4a90e2;
  box-shadow: 0 0 5px rgba(74, 144, 226, 0.5);
  outline: none;
}

input::placeholder {
  color: #999;
  font-style: italic;
}

/* Submit Button */
button {
  display: block;
  width: 100%;
  padding: 12px;
  font-size: 1em;
  background: #4a90e2;
  color: #fff;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-weight: bold;
  transition: background 0.3s, transform 0.2s;
}

button:hover {
  background: #357abd;
}

button:active {
  transform: scale(0.98);
}

/* Responsive Design */
@media (max-width: 768px) {
  form {
    padding: 20px;
  }

  h2 {
    font-size: 1.8em;
  }

  button {
    padding: 10px;
  }
}
</style>

<form method="POST" action="/library/<%= library._id %>?_method=PUT"> 
  <h2>Edit Your Post</h2>
  <p>Post ID: <%= library._id %></p>
  <p>Post Username: @<%= library.Username %></p>
  
  <div>
    <label>Book Name:</label>
    <input type="text" name="Bookname" value="<%= library.Bookname %>" required>
  </div>
  <br>
  
  <div>
    <label>Author Name:</label>
    <input type="text" name="Author" value="<%= library.Author %>" required>
  </div>
  <br>
  
  <div>
    <label>Username:</label>
    <input type="text" name="Username" value="<%= library.Username %>" required>
  </div>
  <br>
  
  <div>
    <label>Mobile No:</label>
    <input type="tel" name="Mobile_no" value="<%= library.Mobile_no %>" pattern="[0-9]{10}" required>
  </div>
  <br>
  
  <div>
    <label>Date Issued:</label>
    <!-- Display only the date in YYYY-MM-DD format -->
    <p style="font-weight: bold;">
      <%= new Date(library.DateIssued).toISOString().split('T')[0] %>
    </p>
  </div>

  <div>
    <label>Last Date to Submit:</label>
    <input type="date" name="LastDateToSubmit" 
      value="<%= new Date(library.LastDateToSubmit).toISOString().split('T')[0] %>" required>
  </div>
  <br>

  <button type="submit">Submit</button>
</form>
</body>
</html>
